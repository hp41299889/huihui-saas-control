import { prisma } from "@/util/server/prisma";

const { service } = prisma;

const createOne = async () => {
  try {
    return
  } catch (error) {
    throw error;
  }
}

const readAll = async () => {
  try {
    
  } catch (error) {
    throw error;
  }
}